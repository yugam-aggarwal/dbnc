# Core (model + pipeline structure)
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.4
torch>=2.2
# Experiments (tuning, datasets, baselines)
optuna>=3.6
openml>=0.14
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2
# Figures
matplotlib>=3.7

# Exact versions used for the paper (see REPRODUCIBILITY.md):
#   Python 3.11, torch 2.5, scikit-learn 1.8, optuna 4.8,
#   xgboost 3.2, lightgbm 4.6, catboost 1.2
